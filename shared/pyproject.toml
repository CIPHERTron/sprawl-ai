[project]
name = "sprawl-shared"
version = "0.1.0"
description = "Shared protocols, refs, and models for Sprawl AI"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["shared"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
